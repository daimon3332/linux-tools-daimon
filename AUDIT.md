# 2026-09-25 Debian 基础工具与兼容修复

新增一级菜单 `20. Debian 基础工具`，原 1–19 编号不变。该菜单固定检查 `ca-certificates curl wget jq` 四项，只安装实际缺失项，不再展示或选择其他软件包。`git`、`python3` 等工具不属于该菜单默认范围；其他菜单安装对应工具或执行功能时，继续由现有安装流程检测缺少的命令与依赖并按需安装，不会因打开菜单而批量安装无关工具。

Debian 判断统一使用 `/etc/os-release` 的 `ID=debian`，界面和文档不再写版本分支名称。编程工具的 Python 选项在 Debian 上安装发行版 `python3`、`python3-venv`、`python3-pip`，不添加 Ubuntu PPA，也不替换或卸载系统解释器；Ubuntu 保持原 Python 3.12 行为。没有 `/var/log/auth.log` 或 `/var/log/secure` 时，fail2ban 的 SSH jail 使用 systemd journal 后端，并在写配置前按需安装 `python3-systemd`。网络持久化缺少 `python3` 时会先安装，安装失败则停止写入。

代码已通过 Linux 语法、菜单分派及回归测试；实测在一台 Debian x86_64 服务器和一个隔离 Debian 环境完成。测试覆盖菜单默认四项、缺包安装、重复执行不调用 APT、非 Debian/非法包拒绝、编程工具 Python 路径、fail2ban journald 配置检查、网络持久化、Docker 未安装提示，以及 77 项通用和 19 项安装回归。普通菜单入口的安全进入/返回路径也逐项执行；系统升级、清理、服务安装、备份还原和退役等有实际副作用的分支没有为测试而强行执行。

测试过程中内置更新流程自动生成了证书续期脚本、定时任务、logrotate 配置、acme 辅助目录，并安装了 `ufw lsof socat python3-venv python3-pip` 及其自动依赖；核对精确路径、归属、时间与内容后，已删除这些测试生成物和测试专用软件包，恢复 root crontab 为不存在。SSH 始终正常，其余服务与测试前状态一致。

# 2026-09-23 一键配置停顿与错误传播修复

用户提供 iperf3 安装结束后停在“按任意键继续”的截图，并指定西班牙服务器验证。基线 `89f5977` 在该机 Ubuntu 22.04 ARM64 的真实 PTY 中复现：批量安装通过 `DAIMON_DEFER_SHELL_RESTART=1` 延后 Shell 重启，但批量函数结束后只返回外层菜单的 `break_end`，没有最终 `exec bash`。原回归只检查能执行到时区步骤，未检查真正进入新 Shell。

修复代码 `a446fb7` 已提交推送，并通过西班牙已安装脚本的内置更新器部署。归一化 SHA256 为 `303f006bd9f36b560bf4da6b5a95073022982e3ec07e58626699878e26dd25e2`，部署前后均核对；未使用 SCP 覆盖生产代码。

本次沿一键配置调用链确认并修复：

- 批量中间暂停、结束后缺少 Shell 重启。现在验证编号、去重、逐项执行，汇总成功与失败，最后仅一次 `exec bash`；直接选择第 9 项同样适用。取消和 EOF 不启动操作。
- DNS、时区/语言、Docker 安装失败被后续统计或配置命令覆盖。新增故障测试在旧版复现，修复后返回真实失败，后续配置仍继续。
- 无效编号混在有效编号后，部分系统操作已执行。现在整批预校验；支持十进制前导零，避免重复执行。
- Docker 安装入口会卸载已有安装、无条件覆盖镜像配置。现在先验证已有 Docker/Compose/daemon，正常则保留，异常则失败；新安装失败不执行后续配置，国外/HK 不写国内镜像，现有 daemon.json 保留。
- Locale 生成失败后仍写 LANG；RHEL 英文设置错误安装中文包。现在检查安装/生成及实际 locale，可用后才更新默认语言；语言包按选择匹配，未知系统返回失败。
- DNS 写入错误可被 chattr 成功掩盖，IP 探测失败时混用国内外回退 DNS。现在按已选区域写入并检查结果；文件系统不支持锁定时明确说明，保留实际写入结果。
- zypper 刷新失败仍继续更新、未知包管理器返回成功，以及 IP/统计请求缺少时间上限。分别补齐返回值和有界请求。

## 西班牙服务器验证

- 16 项新增回归全部通过：真实 PTY 驱动正式菜单/批量/工具分派，只替换安装副作用；覆盖成功、部分失败、完整选项顺序、单独工具安装、空输入/取消、非法编号、去重、中间暂停、同 PID Shell 替换，以及 DNS、Docker、语言和更新失败。原 76 项通用和 19 项安装回归全部通过，总计 111 项。
- 宿主机真实走主菜单 `4 → 1 → 9 10`：执行 15 项工具安装、时区与语言设置，确认 iperf 后继续执行第 10 项、打印成功汇总并进入新 Bash；新 Bash PID 等于原脚本 PID，cpcat、fzf 快捷键和 ble.sh 函数实际加载。
- 同机创建带专用标签的干净 Ubuntu 22.04 ARM64 容器，真实走 `4 → 9` 完成全部 15 项首次安装，包括 Yazi 和 iperf；最终一次 `exec bash` 及三项 Shell 集成检查通过。容器退出 0，核对名称、标签和状态后已移除。
- 实际调用已有 Docker 入口，确认版本、Compose、daemon 可用且未重启。Docker 配置和启动时间、SSH 配置、DNS、容器集合、cron 保持不变；宿主机测试临时更改的时区和默认语言已恢复。
- 使用私有挂载命名空间执行真实 DNS 文件写入，验证 CN、HK、ES 和无 IP 探测结果的回退，宿主机 resolv.conf 哈希不变。区域/IP 输入在此测试中固定，不代表真实 CN 网络安装测试。
- 主脚本及生成 Shell 配置语法、diff 检查通过；菜单审计覆盖 754 个 pattern、93 个 case 和 9 个内嵌 Bash。最初临时函数提取器错误截断 heredoc，修正后才复现正式故障；临时 PTY 驱动的转义错误也在执行前修正，均未计作通过。

范围限制：本轮没有在宿主机执行全量系统升级/清理、改 Swap 或应用网络内核参数；这些分支验证了批量分派和异常传播，不将其称为全套宿主机配置实测。Fedora/RHEL 的语言包分支为隔离命令测试，无该发行版实机。证据位于西班牙 `/var/tmp/daimon-one-click-20260923`，本地 `.tmp/one-click-20260923`；只含本任务生成内容，未提交终端原始日志或主机配置。

# 2026-09-23 最终实机验收：三台均通过

按用户要求单次查询，腾讯、春川、古来的真实双备份均已成功，后续 9 月 22 日的定时 root 任务也全部退出 0。三机当前安装版本均为 `89f5977`，归一化 SHA256 为 `071154c4c157582fcdaed65a272465861d7aea28baa62ea081461e5ee86585b5`。

| 服务器 | 最终代码的最新定时任务（上海时间） | 已验证结果 |
|---|---|---|
| 腾讯云 | 9 月 22 日 04:25:01–05:00:18，2117 秒 | 主备份、二级复制/校验及成功标记通过；11 个容器正常 |
| 春川 | 9 月 22 日 04:45:01–05:59:38，4477 秒 | 主备份、二级复制/校验及成功标记通过；7 个容器正常 |
| 古来 | 9 月 22 日 04:45:01–12:37:21，28340 秒 | 主备份、二级复制/校验及成功标记通过；2 个容器与 `sese.service` 正常 |

古来此前持续运行的完整验收已于 9 月 21 日 16:30:11 成功结束，退出 0，耗时 55826 秒（约 15 小时 30 分）；9 月 21 日 04:45 的同类定时任务被锁跳过属正常并发保护。验收前后容器 ID 集合和 cron 相同，当前恢复清单均已清除；Chromium 原进程仍在运行，个人数据排除策略保留。

额外核对三机最新日志的主备份校验标记、双备份完成标记、退出码和对应时间的成功文件，均通过。腾讯和古来当前容器 ID 仍与验收时相同；春川的 outlook-mail-reader 于 9 月 22 日 11:14（最新备份完成后）被重新创建，因此“当前 ID 必须等于三日前”的附加检查未通过，未将其认作备份恢复故障；当前 7 个容器均运行，带健康检查的均 healthy，原验收期间 ID 集合确实不变。未据此推断容器由谁重建。

当前 cron 与验收保存内容一致，三机主备份、服务和容器待恢复清单均无残留。受管运行日志总量分别约 4.9 MiB、2.9 MiB、9.6 MiB，最大单文件约 3.5 MiB；对应分区剩余约 47、94、98 GiB，inode 充足。日志预算与空间保护已通过前述故障回归，但这些容量是此次查询时的读数，不承诺外部程序不会耗尽磁盘。

本次仅查询并核实结果，没有重跑备份、恢复持续监控或修改业务服务。临时核对脚本首次因生成的正则字符串换行失败，修正后重新执行；该读取错误不影响生产任务。测试记录保留在原隔离证据目录中。此前各阶段“进行中/未通过”均为历史记录，以本节最终结果为准。

# 2026-09-21 古来校验失败修复

按用户要求单次查询发现，古来 9 月 20 日 21:04:39 启动的任务在 23:31:52（上海时间）以退出码 1 结束，阶段为 `primary_check`，尚未执行第二份备份。恢复结果正常：两个容器及 `sese.service` 已运行，恢复清单清除，Chromium 原进程仍在。

实际错误为 `.bash_history: sizes differ`，以及 OneDrive `activityLimitReached: throttledRequest` 导致图片缓存目录读取失败并报告两个文件缺失；其余 37,372 个文件匹配。不能将目录读取失败直接当作已确认的数据丢失，也不能忽略错误宣称成功。

修复 `44d4df5` 将已有私有配置副本扩展到备份范围内的标准 Bash/Zsh 历史文件，上传固定副本并单独校验，保留在线文件的新内容；没有新增数据排除。root 默认每秒 4 次请求、突发 1 次，低层重试 10 次；完整内容校验最多 3 次，等待至少 60／120 秒并尊重日志中更长的重试等待。`89f5977` 明确排除取消、致命错误及传输限制退出码，保持失败恢复语义。

依据（2026-09-21 读取）：[rclone v1.75.1 check 源码](https://github.com/rclone/rclone/blob/v1.75.1/cmd/check/check.go) 使用 `Run(false, true, ...)`，而 [Run 实现](https://github.com/rclone/rclone/blob/v1.75.1/cmd/cmd.go) 的首参数控制高层重试，因此仅传 `--retries` 不会重跑 check。请求平滑使用 [rclone TPS 文档](https://rclone.org/docs/#tpslimit-float)；退避遵循 [Microsoft Graph 限流指引](https://learn.microsoft.com/en-us/graph/throttling)。每秒 4 次是本脚本的保守默认值，不是微软对所有账号承诺的固定额度，多服务器仍需分配总请求预算。

旧版的历史变化和整轮检查不重试已由生成脚本的隔离用例复现失败。首轮修复的多文件目录创建问题也由四机测试发现，`921fe7c` 已修正，不将失败轮次计作通过。华为真实 HTTP 测试确认 17 个请求分布在约 4 秒内且复制内容正确；真实多文件副本测试确认权限、复制/校验、在线文件保留及损坏检测均通过。

`921fe7c` 在华为、腾讯、春川、古来各通过 7 项专项、1 项 root-mounts 迁移、76 项通用回归。覆盖历史追加及配置刷新、副本目录复用、无配置文件时仍备份历史、限速参数校验、重试后的内容一致及持续差异不得成功。

最终代码为 `89f5977`，归一化 SHA256：`071154c4c157582fcdaed65a272465861d7aea28baa62ea081461e5ee86585b5`，四机均经内置更新器部署。四机还各通过最终版本的 2 项重试专项、1 项迁移和 76 项通用回归，确认取消和致命错误不重试。古来本次真实重试采用 4 路传输、8 路检查、每秒最多 4 次请求，沿用已有服务暂停名单和 Chromium 排除项；新一轮于 9 月 21 日 00:59:45（上海时间）启动，已确认私有元数据副本生成、`sese.service` 按授权暂停、Chromium 原进程仍运行。该轮最终成功，详见 9 月 23 日验收记录；未恢复持续轮询。

# 2026-09-20 root 同步与日志修复

本轮只处理通用 root 备份及运行日志；Emby 不在范围内。核心修复为 `d03e24b`，含宿主机服务和并发参数的最终代码为 `8e40f92`，已通过内置更新器部署华为、腾讯、古来、春川。春川先完成 `d03e24b` 的真实任务，再更新到最终版本，未在任务运行中替换代码。最终归一化主脚本 SHA256：`e28348505cbd06b93b8855776b59e05a205b402252baca58d3b3c110e4287732`。更新使用固定提交地址避免 GitHub 分支缓存旧版本。

## 已定位与修复

- 腾讯云先启动应用并等待健康、后启动数据库，造成恢复超时。按 Compose 依赖排序恢复，区分 `service_started` 与 `service_healthy`；启动失败不阻断其他原运行容器恢复。
- 春川遗留恢复清单含已删除容器 ID，阻断后续任务。仅在存活 ID 已恢复、消失 ID 可确认时协调旧清单，持久记录待恢复容器，不盲删记录或启动原本停止的容器。
- 古来清单约 9.5 MB，超过 `/run/lock` 的 5 MB tmpfs。大清单改用磁盘工作目录，小型锁保留原位置；工作、状态、日志目录检查剩余空间与 inode。
- OneDrive 不支持部分目录时间戳更新，root 传输禁用目录时间戳更新并保留文件内容检查。
- 慢启动服务需要使用 Docker 健康配置计算等待期限。主备份校验后先恢复运行，服务预热与第二远端复制并行；最终健康不通过仍判失败。
- rclone 自动刷新 `/root/.config/rclone/rclone.conf`，曾导致已上传文件与在线文件 quickxor 不同。改用临时私有配置副本上传并单独校验，在线配置仍可刷新令牌；二级远端完整复制该副本。
- 宿主机写入者检查尊重排除规则。提供任务旁 `.root-backup.exclude`；用户明确授权古来排除 `/root/snap/chromium`，未停止浏览器或删除数据。
- 日志默认每文件 8 MiB 轮转阈值、总预算 128 MiB、保留 30 天；不删除活动日志或未知业务文件。空间不足时拒绝启动或通知任务取消并恢复服务，日志写入失败不能妨碍恢复。

## 验证记录

- 最新 `d03e24b`：华为云针对性测试通过，包含凭据刷新和按服务器排除 2 项、root-mounts 迁移 1 项及通用回归 76 项。实际 rclone 本地文件测试确认新 `--no-traverse` / `--files-from` 参数能传输和校验配置副本、允许目标其他文件，并能检测内容不同。
- 最新 `d03e24b`：腾讯云和古来完整隔离回归均通过，每台 32 项 root 专项、130 项迁移、76 项通用，共 238 项；不等同于重跑完整云端备份。
- 本地 Bash 语法和 diff 检查通过；菜单检查覆盖 754 个 pattern、93 个 case、9 个内嵌 Bash。初次菜单运行因 Windows PATH 缺少 shfmt/Bash 失败，指定仓库已有 shfmt 与 Git Bash 后通过。
- 此前各阶段已在 ARM64 / x86_64 Linux 上执行恢复、日志满盘、只读、空间/inode 不足、超过 5 MB 清单、外部容器重启、宿主机写入者、永久不健康等隔离回归；不能把早期版本的通过算作最新版实机完整备份通过。

| 服务器 | 真实备份证据 | 当前验收状态 |
|---|---|---|
| 腾讯云 | `d1a7310` 于 2026-09-20 15:08:48–16:46:55（上海时间）完成主备份、二级复制及检查，退出 0；11 个原运行容器恢复，cron 不变 | 早期修复版完整链路通过；最新代码另做隔离回归 |
| 春川 | `d03e24b` 于 18:00:08–20:22:36（上海时间）完整完成双备份，退出 0，耗时 8547 秒；第二远端 14.428 GiB／3130 文件传输及校验通过，7 个容器健康，cron 不变 | 完整链路通过；结束后已内置更新至 `8e40f92` |
| 古来 | 18:38 的任务因写入者重新启动而保护性失败；21:04 的任务于 23:31 校验失败，两轮均恢复服务 | 9 月 21 日新版重试中，尚未通过，详见上节 |

证据位于各机 `/var/tmp/daimon-audit-20260920` 和 `/var/log/rclone/runs`；本地记录在忽略目录 `.tmp/root-audit-20260920`、`.tmp/audit-20260918`。一次验收启动因临时脚本 CRLF 失败，改为 LF 后重启；该失败未执行备份，不计作通过。

## 宿主机服务补充（18:30 上海时间）

用户随后明确授权古来暂停并恢复 `sese.service`，保留数据库。`0fed8d6` 增加通用的 `.root-backup.services` 配置及持久恢复清单，未把服务名写死进脚本。仅暂停原来 active 的配置服务，主备份期间验证仍停止，主备份后及失败退出时恢复；原本停止的服务保持停止。`995c86e` 进一步拒绝不允许手动启动的单元，并在恢复时明确提交 start，替换尚未完成的 stop 作业。

- `0fed8d6` 在华为、腾讯、古来各通过 3 项服务专项、1 项 root-mounts 迁移及 76 项通用回归。覆盖原服务集合保留、恢复失败保留记录、外部重启拒绝、主备份后恢复再复制第二远端、传输失败恢复。
- 华为真实 systemd 测试服务暂停、停写状态检查、恢复及清单清理通过；测试单元链接已核对路径后删除。
- “禁止手动启动的服务仍会被暂停”通过旧版隔离用例复现失败，修复后在华为和腾讯各通过 3 项专项、1 项迁移和 76 项通用回归，并在华为重复通过真实 systemd 测试。古来实际 `sese.service` 的 `RefuseManualStart=no`，不触发此问题。
- 古来 `0fed8d6` 真实任务于 18:24:32 启动，`sese.service` 已按授权暂停，`services.pending` 记录该服务；Chromium 原进程仍在运行。该轮随后于 18:35:13 为调整验收并发正常取消，服务恢复通过。
- 春川 `d03e24b` 本轮主备份内容检查已通过，18:16:57 进入第二远端复制，7 个原运行容器已恢复健康。该轮于 20:22:36 成功完成，随后通过内置更新器升级。

## 首次大批量传输与中断恢复

古来清单实际包含 38,194 个文件，其中 36,253 个位于 `sese`。默认四路传输对大量小文件约每分钟完成百个，因此字节进度接近 100% 不代表任务即将完成。为执行用户加快验收的要求，`8e40f92` 增加受限环境参数 `DAIMON_ROOT_TRANSFERS`（1–32，默认 4）和 `DAIMON_ROOT_CHECKERS`（1–64，默认 8）；不改变原 cron 和默认行为。

- 对古来旧验收任务的确切主脚本进程发送 TERM，18:35:13 正常返回 143；原 2 个容器与 `sese.service` 均恢复，恢复清单清除，Chromium 原进程仍存活。取消附近有一次 OneDrive 409，未计作成功，将由重试同步和内容校验处理。
- `8e40f92` 经内置更新部署华为、腾讯、古来，归一化 SHA256 为 `e28348505cbd06b93b8855776b59e05a205b402252baca58d3b3c110e4287732`；每台 4 项专项、1 项迁移、76 项通用回归通过，含非法并发参数不得暂停服务。
- 古来于 18:38:41 以 16 路传输、32 路检查启动实际重试，复用远端已上传文件；服务暂停与最终恢复仍由正式生成脚本负责，测试监督进程只记录结果。

## 停止持续监控后的交付状态

用户明确表示可以等待，要求不再持续监控。已停止主动轮询，两机任务由 systemd 在后台运行。后续应用户要求单次查询，确认春川已成功、古来仍执行；古来不提前计为通过。两任务的单次运行上限放宽到 24 小时，保留 15 分钟退出恢复期，主进程 PID 均未改变。当前 systemd 拒绝在线设置 RuntimeMaxUSec，改用这两个测试单元专用的 `/run` drop-in 后重新加载，实际属性均验证为 `RuntimeMaxUSec=1d`；未重启任务或修改正式 cron。

停止轮询前，两任务出现同步的短暂传输停顿；只读远端探测正常，古来记录一次可重试的 OneDrive 409，随后计数继续增加，不能据此断言云端没有错误或已完成。古来当时根分区约 99 GiB 可用，12 个运行日志合计约 2.9 MiB，两远端各约 1 TB 可用；这些是当时的检查结果，不保证其他程序不会继续消耗容量。

最终任务仅在两端内容检查、主备份代次检查和服务恢复全部通过后写入成功状态；重试后仍失败会记录失败并尝试恢复服务，恢复失败保留清单。春川更新后已核对主脚本哈希、生成任务一致性、原容器集合及健康、cron、当次成功标记及恢复清单清除，并通过最终版本的 4 项专项、1 项迁移和 76 项通用回归；古来最终结果仍待后续按需查询。

## 古来外部启动写入者后的重试

用户报告可能误停止任务后，单次核对确认古来于 20:53:18（上海时间）退出 1，原因是 `Service restarted during primary backup: sese.service`。当时累计传输约 15,041 个文件，尚未完成主备份校验；脚本停止传输并恢复服务，`recovery_failed=0`，两个容器和 `sese.service` 均运行，恢复清单清除。该结果证明外部重新启动写入者会触发一致性保护，不能将本轮标为备份成功，也不能仅凭日志认定服务由谁启动。

沿用原授权于 21:04:39 启动新一轮真实验收，保留此前失败证据、已上传数据、16/32 并发、24 小时时限及 Chromium 排除策略；主脚本哈希及生成任务比较通过。新任务已记录执行中并暂停 `sese.service`，后续重新同步变化文件、校验并恢复服务。未开启持续监控。

## 适用范围

不针对云厂商或 CPU 写死分支，但依赖 Linux、Bash、Python 3.9+、util-linux、rclone，以及涉及容器时可用的 Docker daemon。root 文件镜像不等于整机镜像，范围外的 Docker named volumes 仍需独立备份。不能保证检测到所有短暂宿主机写入，也不能保证其他程序不会占满磁盘。日志限制是周期监控，突发输出可能短暂超预算。空间不足、无法确认停写或最终服务不健康都会明确失败，不宣称适用于任意未知环境且绝无故障。

# 2026-09-18 网络持久化故障闭环

用户补充准确症状为“应用成功，重启或重载后失效”，此前仅验证运行态的结论不足。已在旧版 `6fe8c0f` 复现：新加坡重载后 `tcp_max_syn_backlog` 从 262144 回到 8192，华为云回到 1024，`swappiness` 从 10 回到 0。

原因由 [systemd sysctl.d 手册](https://www.freedesktop.org/software/systemd/man/latest/sysctl.d.html) 和 [procps sysctl 手册](https://man7.org/linux/man-pages/man8/sysctl.8.html) 说明并通过实机证实：systemd 按文件名排序；procps `--system` 还最后读取 `/etc/sysctl.conf`。因此只调整一个 sysctl.d 文件名无法同时解决两条路径。

代码 `f0f0696` 保留原始配置，增加受管的 `zz-daimon-network.conf` 与 sysctl.conf 末尾块；BBR、应用、清除三个入口同步维护。重复应用、优先级、文件碰撞、损坏标记、写入失败均有回归覆盖。初次实测发现验证器拒绝 `fs.file-max` 的连字符，已修正后重新部署验证；失败期间运行态和文件恢复成功。

- 四机已通过真实 procps `sysctl --system`、systemd 启动加载器、`sysctl -p` 验证。业务机使用私有挂载视图，仅向加载器提供本次涉及的参数，避免重载其他服务的设置；保持真实文件顺序与重复定义，运行态实际写入并逐项校验，结束恢复原参数和五个配置文件。
- 华为云作为用户授权的闲置测试机，实际重启，boot ID 已变化。重启后逐项参数及 BBR/FQ 检查通过，随后全量 `sysctl --system` 通过。
- 华为云清除优化后原有 `tcp_max_syn_backlog=1024`、`swappiness=0` 恢复，BBR/FQ 保留；最终测试前配置和参数恢复检查通过。其余三机没有重启。
- 四机脚本通过内置更新部署，代码归一化 SHA256 为 `77d4ebe182d5fa0368e6f08e34b2f6dc5c72a5c2e62275d3f935db27efa2878f`。GitHub 分支同步曾延迟，哈希不符时未测试旧版，改用内置更新函数下载固定提交版本。
- 本机为 Windows，新增持久化文件权限测试明确跳过，在四台 Linux 上执行。每台 10 项持久化测试（含第二文件替换失败回滚、用户符号链接与权限保留、拒绝不安全覆盖路径）、76 项通用及 19 项安装回归均通过，共 105 项；菜单分派与内嵌 Bash 语法检查通过。证据位于原 `audit-20260918` 任务目录；华为云重启证据为 `network.WTijQZ`。

此修复保证已应用配置在上述加载路径下保持优先级。其他程序随后主动执行 `sysctl -w` 或管理员再追加覆盖项，仍可改变内核参数；不使用循环守护进程争抢设置，也不承诺吞吐一定提高。四机原参数已恢复，需要使用新版菜单 `5 → 15 → 1` 应用持久化优化。

# 2026-09-18 安装与网络复查（此前记录，网络未决项已由上节解决）

代码基线 `aa2051e`，最终代码 `6fe8c0f`。修改均提交推送后，使用已安装脚本的内置更新菜单或内置更新函数下载部署；没有用 SCP 替换生产脚本。四机最终普通菜单 `00` 更新成功，排除许可、地区、统计三行并去除 CR 后，脚本 SHA256 均为 `6fbfa3ccf152a111ce962ec23b25e568e3637b881b69f557f6188a5281df0cb4`。

## 已复现并修复

- Yazi 旧密钥地址返回 301，`curl -sS` 未跟随跳转，GPG 报 `no valid OpenPGP data found`。改用 Yazi 官方签名 APT 源，安装后实际执行 `yazi` 和 `ya`。
- 工具依赖、克隆、编译及包安装失败被后续命令覆盖，或者失败后仍写入 Shell 配置。补充显式失败返回和执行验证；fzf 配置加载快捷键，ble.sh 安装补齐 gawk。
- 国内下载只代理外层，Git 子模块、fzf 安装器内部发行包和 NextTrace APT 地址仍直连。扩展这些下载路径的 CN 分流；HK、SG、JP 保持直连。
- 缓存镜像和 GitHub 分支 CDN 均曾返回旧脚本，但旧更新器仍提示成功。更新优先仓库、国内优先代理，并添加防缓存查询参数。发现哈希不符的测试未计为新版通过。
- 本轮单源 APT 刷新曾清除其他源索引，真实后续安装 Python 提示找不到包。添加 `APT::Get::List-Cleanup=0` 后，真实安装与索引保留复验通过。
- 严格 pipefail 环境下，代理选择的 `head -1` 提前关管道造成失败；改为读取完整候选流，确定性回归由失败转为通过。
- Ubuntu 24.04 卸载 Python 3.12 会移除系统依赖；新增系统解释器保护。ClaudeCode/Codex 安装保留现有配置。

官方依据（2026-09-18 读取）：[Yazi 安装说明](https://yazi-rs.github.io/docs/installation/)、[官方 APT 仓库](https://github.com/yazi-rs/builds)、[v26.9.1 发行资产](https://github.com/sxyazi/yazi/releases/tag/v26.9.1)。第三方 Griffo 页面与官方文档对 APT 可用性的描述存在差异，采用官方文档并实测签名仓库与二进制。

## 实机结果

| 环境 | 安装与终端 | 回归 | 网络菜单 |
|---|---|---|---|
| 古来，Ubuntu 22.04 ARM64 | 独立 Ubuntu 容器，15 项第三方工具首次/重复安装通过，Bash/fzf/ble.sh 和 Yazi TUI 通过 | 76 通用 + 19 安装 + 130 迁移通过 | `5 → 15 → 1` 应用、验证、恢复通过 |
| 新加坡，Ubuntu 22.04 ARM64 | 同上 | 76 + 19 通过；宿主机无 rclone，迁移套件未执行 | 应用、验证、恢复通过；发现 sing-box sysctl 覆盖项 |
| 日本，Ubuntu 22.04 x86_64 | 同上 | 76 + 19 通过；宿主机无 rclone，迁移套件未执行 | 应用、验证、恢复通过 |
| 华为云，Ubuntu 24.04 x86_64，CN | 宿主机 15 项安装通过；Yazi 卸载/重装通过；重复安装、终端、索引保留与系统 Python 保护通过 | 76 + 19 + 130 通过 | 应用、验证、恢复通过；发现 sysctl.conf 覆盖项 |

华为云初始根分区仅余约 71 MB。用户授权作为测试机后，仅清理核实过路径的 `/root/.npm/_cacache` 下载缓存，未清空系统或业务目录。其余三机工具安装在带专用标签的独立测试容器中，未重装宿主机工具。终端测试使用真实 PTY，验证 Bash 集成函数、Yazi 启动与正常退出。

主脚本与测试 Bash 语法、`git diff --check` 通过；菜单检查覆盖 754 个 pattern、93 个 case 块、9 个内嵌 Bash 脚本，仅代表语法/分派覆盖。ShellCheck 完整扫描仍有既有警告，不声称零警告或全部菜单业务实测通过。

## 未决与限制

- 用户报告的网络优化“完全无法使用”在四机旧版和新版均未复现；完整菜单实测也成功。新加坡 `/etc/sysctl.d/99-zz-sing-box-daimon.conf`、华为云 `/etc/sysctl.conf` 定义了不同参数，重启/全量重载可能覆盖本模块。保留这些外部配置，等待原始失败表现；未宣称根因已修复或网速一定提升。
- 四机当前都已有 BBR 和 fq / mq+fq；未在宿主机替换未知整形队列或更换内核。网络参数、持久化配置和 qdisc 在测试后恢复核对。
- 香港仅验证分流逻辑，未提供香港实机。国内实测为华为云 CN x86_64，不代表所有中国线路或国内 ARM64。
- 本轮“全部安装”实测指 15 项第三方工具；编程工具整组安装、破坏性菜单、生产备份还原和外部一键脚本未全量执行。
- 服务器证据目录为 `/root/linux-daimon/tests/audit-20260918`，本地证据为忽略目录 `.tmp/audit-20260918`。测试环境异常及中途失败保留记录，最终结果以新版复验为准。

# 2026-09-07/08 脚本审计

## 范围与方法

基线为 `e655c21`。审查主脚本、菜单和 CLI 分派、内嵌 Bash 脚本、下载与失败处理；在中国、古来、新加坡三台获授权机器验证。所有生产代码修改均提交推送，再使用主菜单 `00` 更新测试机；比较 SHA256 时仅排除本机保留的许可、地区参数及统计开关三行。

测试层次必须区分：分支匹配检查不执行分支中的系统命令；mock 回归验证指定失败路径；实机仅执行明确记录的安全操作及隔离安装。未在业务服务器真实执行系统升级、卸载、格式化、账号删除、证书签发或业务数据还原，不能据此声称所有功能实机通过。

## 菜单覆盖

以下选项均检查分派及调用关系；具体分支清单可运行 `tests/menu-audit.mjs --inventory` 生成，包含主菜单、子菜单、CLI、内嵌脚本和部分数字辅助分支，不应将总数称为独立功能数。

| 主菜单 | 逐项检查范围 | 验证与限制 |
|---|---|---|
| 1 系统信息 | 系统、资源、网络及服务状态 | 实机只读查询；比对 SSH、rclone、cron、路由、qdisc 未变 |
| 2 系统更新 | dpkg 修复、软件源、升级调用 | mock 锁占用与错误传播；未真实升级系统 |
| 3 系统清理 | 包清理、日志、临时文件 | 锁与失败路径回归；未在业务机执行清理 |
| 4 一键配置 | 1-10、0；批量 2-10 | 分派、EOF、失败汇总、第三方工具后继续执行时区；实机只进入/返回 |
| 5 系统工具 | 1-20、0；DNS、Swap、账号、时区、网卡等子菜单 | 快捷键碰撞、用户名、Swap 失败恢复、TCP 回滚回归；实机安全导航 |
| 6 第三方工具 | 16 个工具；安装/卸载/全部安装/全部卸载/返回 | 16 个 ID 逐项分派，08、非法编号和批量失败；未全部真实安装卸载 |
| 7 编程工具 | 分类中的全部 ID 和安装/卸载入口 | 静态分派及实机安全导航；未更换生产语言环境 |
| 8 Docker | 1-12、19、20、0；容器、镜像、网络、卷、定时更新子菜单 | 静态检查及安全导航；未删除容器、镜像或卷 |
| 9 SSH | 1-6、0；密钥管理 1-4、0 | 放行失败不得重启；端口检测、私钥路径保护；实机只读状态 |
| 10 UFW | 1-4、0 | 未知 SSH 端口/allow 失败不得启用；未切换生产防火墙 |
| 11 Nginx/域名 | 内嵌脚本 1-13、0 | 完整包装入口导航及失败注入回归；古来隔离测试页创建/删除并用真实 Nginx 检查配置语法；未签发证书或改生产服务 |
| 12 fail2ban | 1-3、0 | 静态检查及实机安全导航；未重装服务 |
| 13 BBR | 下载、缓存、内核能力和外部管理入口 | 能力/队列/失败路径检查；不执行第三方换内核操作 |
| 14 WARP | 1、2、0 | 静态检查及实机安全导航；未安装或删除网络接口 |
| 15 rclone | 1-6、0；恢复选择子菜单 | CN AMD64、ARM64 发行包及海外官方安装实测；未还原业务目录 |
| 16 Bitwarden | 1-4、0 | 备份退出码、配置验证与 token 隐私回归；未覆盖生产备份或配置 |
| 17 crontab 同步 | 1-5、0；4 个内置脚本 | 分派、脚本语法及安全导航；未运行云端 sync 或替换业务任务 |
| 18 常用脚本 | 1-12、0 | 下载入口、分派与安全导航；未执行所有第三方脚本 |
| 00 更新 | 下载、校验、取消、替换、重新进入 | 三机通过内置更新；坏语法拒绝、旧目录副本不覆盖新版 |
| 0 退出 | 退出与主菜单 EOF | 实机及回归验证 |

## 已修复问题

- 国内 rclone 只代理外层脚本，内部发行包仍慢速直连：CN 改为代理下载版本化发行包，校验后原子安装；海外保留官方安装器，兼容退出码 3。
- 下载失败可留下半截缓存；新增临时文件、非空验证、原子替换及超时。
- 启动优先读取工作目录中的旧脚本，并截断正在运行的文件：现在只安装实际执行的源文件，已安装入口不重复覆盖，更新使用原子替换和语法检查。
- 旧目录迁移可能删除源数据；改为非覆盖复制，不删除源目录，不盲目改写用户 crontab 或 shell 配置。
- 包管理锁被强杀或删除；安装前一项失败被后一项成功覆盖；均改为失败即停止。
- Swap 原逻辑会重新格式化其他 swap 分区；现在只管理带 inode 标记的文件，停用失败不覆盖，启用失败恢复旧文件。
- UFW 放行失败仍启用，SSH 改端口先重启后放行；调整顺序并检查失败。私钥文件名不能穿越目录、覆盖保留文件或已有文件。
- 批量第三方工具安装 `exec bash` 导致后续时区步骤不执行；增加延迟重启及失败汇总。
- 菜单 EOF 仍循环或使用默认项；交互读取失败现在停止当前操作。
- Bitwarden 仅凭日志标志判断成功、打印 token 并检查固定前缀；改为退出码与标志共同判断，配置暂存并实际连接远程后再替换。
- TCP 只检查默认 qdisc 字段，回滚只执行全量 sysctl 重载；现在检查实际队列、保存运行态并逐项恢复验证。
- 自定义快捷键可覆盖系统程序，用户管理输入可指向系统账号或路径；增加碰撞、用户名、UID 和删除确认保护。
- 分区卸载按挂载路径匹配分区名而查不到设备；改为 `findmnt -S`，卸载后不删除用户挂载目录。
- Nginx 菜单进入即安装依赖；改为仅申请证书或明确更新续期组件时安装。
- 续查确认部分清理分支会吞掉失败、清空共享 `/tmp` 和 `/var/log`，并用 1 秒保留期删除 journal；改为失败即停止，仅使用包管理器清理和原有 500M journal 上限。
- Nginx 内嵌函数在条件调用链中不能依赖 `set -e`：依赖、下载、服务启动、配置写入和续期失败现在显式返回；acme 下载设置超时并检查管道状态，不执行用户 shell 初始化文件。
- 测试页只判空导致名称穿越和端口注入，删除菜单也会误列同名业务目录；新增输入及路径检查、拒绝覆盖已有目标，仅删除带 `.daimon-test-page` 归属标记的测试目录。未标记的历史目录保留。

## 本轮验证（`fa715a3`）

- 本地、国内、古来、新加坡的完整隔离回归均为 **67 passed, 0 failed**。新增清理/Nginx 用例已在旧版 `c75d3c7` 复现失败，再验证修复；不是只检查正常退出。
- 菜单审计通过 **723 个 pattern、88 个 case 块、12 个内嵌 Bash 脚本**；主脚本、回归脚本及内嵌 Bash 语法通过。pattern 只检查分派匹配，不代表 723 个功能完成业务实测。
- 三台均通过主菜单 `00 → 1` 内置更新。`/usr/local/bin/d` 和 `/root/linux-daimon/linux-toolbox.sh` 删除 CR、排除 `canshu`、`permission_granted`、`ENABLE_STATS` 三行后，SHA256 均为 `88c67c91e0d728eacdba041af81b4da8e88395ecc615de383bacd22d385d1a54`。
- 三台安全实测覆盖旧工作目录启动保护，菜单 4、5、6、7、8、9、10、12、14、15、16、17、18 的非法输入、返回、EOF，以及系统信息查询。最终 `sshd -t` 通过，SSH/rclone 程序文件、cron、路由和 qdisc 与安全快照一致；rclone 配置的末次检查结果见下项。
- 文档提交 `00cc7aa` 后再次内置更新：古来、新加坡的配置保护检查通过；国内机脚本哈希正确，但 `rclone.conf` 在更新窗口内变化，首次保护检查失败。只读检查发现此前已运行的 `rclone copy` 作业，配置修改时间与 OAuth 令牌时间吻合，符合并发刷新；未取得首次写入者事件日志，旧快照又只有文件哈希，不能追溯断言仅 token 字段变化。保留当前凭据和失败记录，未停业务作业、未回滚配置；使用 strace 只跟踪更新进程及其子进程复测，未发现配置写入/替换，复测前后配置哈希一致，证据保存在任务目录 `cn-update-00cc7aa-trace/`。该复测不将首次失败改记为通过。
- Nginx 完整包装入口的返回、非法输入、EOF 由三台隔离回归覆盖，验证不安装依赖、不修改服务或 cron。古来另验证测试页创建、拒绝重复创建、受管删除及未标记业务夹具保留；使用真实 `nginx -t` 检查生成配置，安装和 reload 使用 mock，未改生产 Nginx 服务。

## rclone 实测

| 环境 | 测试 | 结果 |
|---|---|---|
| 中国 AMD64 | 同版本 1 MiB Range 下载对照 | 官方两次 12 秒超时，仅取得 304/256 KiB；代理 3.80/5.43 秒完成 |
| 中国 AMD64 | 更新后的发行包安装函数 | 87 秒完成 `v1.75.1` 下载、SHA256、ZIP 和执行验证；原程序及配置校验值不变 |
| 新加坡 ARM64 | 强制 CN 路由，真实 ARM64 发行包 | 4 秒完成并执行 `v1.75.1`；这是架构/代理路径验证，不是中国 ARM 机测速 |
| 新加坡 ARM64 | 真实海外官方安装器，预置旧版本遮蔽 | 9 秒完成；`/usr/local/bin` 与 `/usr/bin` 新程序一致；系统外层文件不变 |

安装目标使用任务目录或私有挂载命名空间。海外测试初次因隔离环境缺少 unzip、临时目录权限而失败；补齐任务目录中的解压工具并修正隔离目录权限后复测通过，未把该环境问题算作安装成功。

## TCP 对比

- tcpfit `v0.5.7`，固定提交 `1163c20e88a4a7130ef7d885da8be3a163505003`。
- TcpQuality 固定提交 `c2295ae096437859ce4bbc36f170428fcac47be9`，核心脚本 SHA256 为 `a7274458ddd785d637b526e2d337b80326aac1d1663414490f2a20ee161ba5c0`。
- 每台顺序为现有 A1、tcpfit B1、tcpfit B2、现有 A2。每台内部固定北京/上海/广东三网节点；两台部分节点 IP 不同，不做跨服务器直接排名。
- B 使用 `tune --role proxy --bw 1000 --rtt 150 --no-initcwnd`。1000 Mbps 和 150 ms 是试验输入，不是已测得的套餐/RTT；未测试所有角色、带宽参数和 initcwnd 设置。
- 单方向请求 64 MiB，最多 10 秒；关闭 AppleCDN、eBPF 和排名上传，不运行全量整形扫描。测速进程移除 NET_ADMIN/SYS_ADMIN 能力。
- 每轮保存 sysctl、路由、qdisc、iptables、CPU 和网卡流量；独立 600 秒回滚 guard，结束后验证恢复。两台均无测试/guard/本地 HTTP 服务残留。
- 两台基线都已有 BBR、mq+fq 和 128 MiB TCP 窗口上限，并非小窗口出厂状态。A 的 TCP 缓冲区为 `4096 131072 134217728`，B 为 `4096 1048576 39597152`。

下面每格为同方案第 1/2 次结果，单位 Mbps；`失败` 不作 0 处理。数值为短时采样，不能当持续可用带宽。

### 新加坡

| 节点 | A 上传 | B 上传 | A 下载 | B 下载 |
|---|---|---|---|---|
| 北京电信 | 30.6/11.4 | 53.7/24.0 | 0.2/0.6 | 0.2/0.7 |
| 北京联通 | 112.6/176.4 | 140.9/259.8 | 2.9/118.2 | 0.1/0.9 |
| 北京移动 | 8.0/53.7 | 28.2/19.9 | 81.0/1.4 | 100.9/1.4 |
| 上海电信 | 失败/18.6 | 0.8/0.6 | 失败/0.0 | 失败/0.1 |
| 上海联通 | 75.0/78.8 | 58.8/61.5 | 2.4/0.2 | 0.2/0.2 |
| 上海移动 | 19.7/79.2 | 12.7/93.8 | 139.5/146.8 | 60.3/56.4 |
| 广东电信 | 8.3/6.0 | 53.4/0.9 | 3.0/1.6 | 2.1/2.9 |
| 广东联通 | 70.2/59.4 | 68.8/72.2 | 78.0/59.8 | 4.3/3.4 |
| 广东移动 | 492.5/465.0 | 452.2/480.6 | 74.7/80.7 | 79.8/123.8 |

### 古来

| 节点 | A 上传 | B 上传 | A 下载 | B 下载 |
|---|---|---|---|---|
| 北京电信 | 21.0/34.4 | 1.6/55.8 | 7.8/101.1 | 25.7/74.3 |
| 北京联通 | 316.2/305.5 | 166.6/271.7 | 0.8/69.7 | 0.3/194.6 |
| 北京移动 | 0.5/1.8 | 5.3/1.4 | 133.0/100.2 | 153.0/147.0 |
| 上海电信 | 1.4/39.2 | 47.3/20.5 | 74.6/28.6 | 1.4/67.1 |
| 上海联通 | 81.8/94.7 | 75.4/74.4 | 60.4/0.2 | 0.4/127.8 |
| 上海移动 | 20.2/2.5 | 1.2/3.4 | 110.2/35.9 | 2.8/126.2 |
| 广东电信 | 7.4/3.4 | 7.4/19.1 | 失败/34.6 | 40.2/59.4 |
| 广东联通 | 76.6/104.5 | 88.4/53.7 | 0.2/0.2 | 0.2/0.6 |
| 广东移动 | 5.5/2.3 | 47.4/11.4 | 2.4/26.2 | 101.3/29.2 |

### 其他指标与结论

- 古来北京联通四轮上传重传均为 0%，但 B 上传两次都低于 A；古来北京移动重传 A 为 28.44%/27.61%，B 为 25.08%/29.30%，没有稳定降低。新加坡广东移动四轮重传均为 0%。
- 使用原始 `.meta` 核对连接耗时。古来北京联通 `connect_ms` 按 A1/B1/B2/A2 为 85/180/113/88；TLS 指标为 222/402/241/214 ms。旧九节点测试未记录传输期间 ICMP RTT，不能将一次连接或 TLS 指标当作负载下 RTT。
- 上游 CSV 表头与测速数据不一致：`发送` 是上传 Mbps，`收到` 是上传重传率，`丢包率(%)` 列实际存下载 Mbps。CSV 连接列又是原始 `.meta` 中 `connect_ms` 的两倍，因此未用该列作为 RTT 结论。
- 新加坡整机 CPU busy 四轮为 32.53%/32.69%/32.16%/32.53%；古来为 33.20%/33.36%/33.60%/33.29%，没有显著 CPU 优勢。是整机区间采样，包含原有业务，不能归因给调优脚本。
- 四轮网卡收发合计新加坡约 2.56 GiB、古来约 2.66 GiB；同样包含区间内业务流量，不是逐请求流量核算。

### 短窗口 RTT 补测（2026-09-08）

仅在古来和新加坡执行，国内机不调优。每台固定先前缓存的北京联通主节点，顺序仍为 A1 → B1 → B2 → A2；两台节点 IP 不同，不跨机排名。核对旧 A/B 的路由和 qdisc 相同后，仅临时应用已保存的 sysctl 参数，不重跑安装器或整形。

每轮先空闲 ping，再分别上传、下载；每方向最多请求 64 MiB、10 秒，ICMP 间隔 0.2 秒。下表 RTT 每格为 **均值 / p95 / 标准差（样本数）**，单位 ms；吞吐单位 Mbps。

| 环境/轮次 | 空闲 RTT | 上传期间 RTT | 下载期间 RTT | 上传 / 下载 |
|---|---|---|---|---|
| 古来 A1 | 125.625 / 128 / 4.872（24） | 113.900 / 128 / 12.761（9） | 79.482 / 82 / 1.182（11） | 246.994 / 258.064 |
| 古来 B1 | 78.912 / 79.3 / 0.287（25） | 78.775 / 78.8 / 0.043（8） | 79.078 / 80.2 / 0.524（9） | 335.469 / 305.120 |
| 古来 B2 | 82.724 / 107 / 8.408（25） | 118.400 / 128 / 12.719（8） | 96.222 / 128 / 16.395（50） | 355.827 / 6.515（部分传输） |
| 古来 A2 | 78.804 / 78.9 / 0.156（25） | 78.938 / 79.9 / 0.377（8） | 79.088 / 82.8 / 0.937（17） | 348.398 / 159.589 |
| 新加坡 A1 | 107 / 107 / 0（25） | 107 / 107 / 0（11） | 107 / 107 / 0（15） | 256.874 / 179.912 |
| 新加坡 B1 | 107 / 107 / 0（25） | 107 / 107 / 0（11） | 107 / 107 / 0（15） | 259.849 / 185.404 |
| 新加坡 B2 | 107 / 107 / 0（25） | 107 / 107 / 0（12） | 107 / 107 / 0（14） | 243.512 / 198.674 |
| 新加坡 A2 | 107 / 107 / 0（25） | 107 / 107 / 0（10） | 107 / 107 / 0（26） | 285.583 / 105.158 |

- 古来 B2 下载在 10 秒窗口取得 8,142,848 字节，HTTP 206、curl 退出码 28；6.515 Mbps 是部分传输吞吐，不能当作零或完整下载成功。其余 15 次方向传输完成 64 MiB、curl 退出码 0；8 个独立上传测试对象均删除成功（HTTP 204）。
- 新加坡 ping 输出仅保留整毫秒，样本均为 107 ms；标准差 0 只表示该输出精度下未分辨差异，不能声称网络没有抖动。传输期间每组仅 8–50 个样本，p95 不能推广为长期尾延迟。
- 古来空闲基线从 A1 的 125.625 ms 变为 A2 的 78.804 ms，表明时段波动明显。测试未暂停既有业务，短传输也不保证持续打满链路，不能将差异全部归因于 TCP 参数。
- 两台逐轮及最终恢复检查均通过：34 项原始 sysctl 参数、路由、qdisc 一致；独立 600 秒回滚 guard 已退出，本地上传载荷已删除，无测速/guard/任务 HTTP 进程残留。未修改持久 TCP 配置。

结论：保留现有调优方案，并修复其验证和回滚。tcpfit 在部分节点/方向上有收益，但九节点 A/B 及本轮短窗口补测均未证明稳定全面胜出。不是断言 tcpfit 无效；要进一步决定特定业务的参数，仍需真实带宽/RTT、更多时段重复样本及长期业务负载下的延迟测试。

## 复现与剩余限制

```bash
bash tests/regression.sh
node tests/menu-audit.mjs /path/to/shfmt --inventory
bash -n linux-toolbox.sh
git diff --check
```

使用 shfmt `v3.14.1` 解析。ShellCheck `0.11.0` 对 `fa715a3` 主脚本的结果为 **0 error、218 warning、526 info、22 style**；单独提取的 Nginx 子脚本为 **0 error、6 warning、23 info**。警告涉及历史变量、引用、cd 和声明风格等，Nginx 包括未使用的 `FOLDER` 和 `local name=$(...)`；未为清零进行无关重构，不能称为零告警。

本机 PowerShell 需先设置 `$env:BASH_BIN = 'C:\Program Files\Git\bin\bash.exe'`，再给菜单审计传入 `shfmt.exe` 路径。直接调用默认 `bash` 会命中 Windows/WSL 入口；本轮该环境性失败在显式指定 Git Bash 后复测通过，未改生产代码。

隔离回归不是业务联调：Bitwarden 的真实容器/远程授权、全部第三方安装器、非 Ubuntu 包管理器、云安全组、所有错误注入组合及跨重启恢复未全部实测。无全量整形扫描；没有修改两台服务器的持久 TCP 方案。

原始日志、CSV、debug 包、菜单清单、前后快照保存在本地 `.tmp/audit-20260907/` 和授权服务器 `/root/linux-daimon/audit-20260907/`，不提交包含服务器状态的原始文件。新补测记录分别收取到本地 `kulai/loaded-latency-20260908/` 和 `sg/loaded-latency-20260908/`，未混入或覆盖旧九节点结果；远程回归与安全导航证据分别为 `regression-fa715a3-verified.log`、`smoke-fa715a3.log`。

参考：

- [rclone 官方安装器](https://rclone.org/install.sh)、[官方安装文档](https://rclone.org/install/)
- [tcpfit 固定源码](https://github.com/Kylin010/tcpfit/tree/1163c20e88a4a7130ef7d885da8be3a163505003)、[用户提供的帖子](https://www.nodeseek.com/post-865242-1)
- [TcpQuality 固定源码](https://github.com/ibsgss/TcpQuality/tree/c2295ae096437859ce4bbc36f170428fcac47be9)
- [Linux 6.8 IP sysctl](https://docs.kernel.org/6.8/networking/ip-sysctl.html)、[Linux net sysctl](https://docs.kernel.org/admin-guide/sysctl/net.html)、[tcp(7)](https://man7.org/linux/man-pages/man7/tcp.7.html)

## 迁移代码修复与双机验证（2026-09-08）

本轮只处理迁移脚本，Mihomo 安装和 DNS 切换由用户负责。保留 Vaultwarden named volumes，不修改业务 Compose，不执行生产备份、云端同步或生产数据恢复。代码提交为 `82f24d3`、`17458e3`。

### 修复内容

- rclone 管理与恢复不再写死 remote；隔离配置副本读取验证，区分有效、认证无效、无法检测，不显示 token。读取成功不能证明备份可写。
- 目录列表改用 JSON，保留连续空格，修复 `08` 编号；恢复增加路径、符号链接、活动挂载、空间检查，暂存并用 `rclone check --download` 核对后再合并。替换失败回滚；并发出现目标时保留回滚副本。
- Nginx 本地与远程恢复共用事务逻辑，补齐依赖，按启用清单建链接，检查证书期限、私钥配对及实际服务状态；失败恢复原配置和原先停止状态。相对软链接可重复恢复，不猜测启用其他站点。
- Compose 使用实际项目 labels 和常见配置文件名，检查缺失 bind/named volume、代理和健康状态；查询失败不执行启动，已健康项目不重建。真实 Docker 测试补充修复 `IPAM.Config=null` 被误报为代理无效的问题。
- Vaultwarden 根据实际挂载选择配置与数据卷，通过备份镜像的 `--entrypoint rclone` 验证，避免上游包装入口吞掉失败。保留其他 remote，检测并发配置变化；ZIP 隔离解密、SQLite/归档校验后恢复已停止的普通 local named volume。
- 生成的同步脚本原子写入并传递失败。现有生产生成脚本没有被重写或执行，需以后通过原菜单重新配置。

### 验证结果

两机为用户指定的华为云与腾讯云，均为 x86_64、rclone 1.75.1；Docker Compose 分别为 5.0.1、5.5.1。每次推送后使用内置 `00 -> 1` 更新，没有覆盖生产主脚本。华为云第一次更新读到旧内容，哈希验证拒绝将其计入新版结果；再次内置更新后匹配。最终两机 `/usr/local/bin/d` 和 `/root/linux-daimon/linux-toolbox.sh` 去除部署保留字段后的 SHA256 均为 `933397dd611c368212e94c4ca61fbc704def60393dfde47c4c8765d2d7d7886b`。

| 检查 | 结果 |
|---|---|
| 新迁移回归 `bash tests/migration-regression.sh` | 两机分别 46 passed, 0 failed；新增 IPAM 测试在前一提交失败、修正版通过 |
| 原回归 `bash tests/regression.sh` | 本地和两机分别 67 passed, 0 failed |
| 语法和菜单审计 | `bash -n`、`git diff --check` 通过；729 patterns、90 case blocks、11 embedded Bash scripts |
| rclone 实际恢复 | 华为云只读 HTTP 源经 SSH 隧道供腾讯云读取；含连续空格目录、隐藏文件及二进制文件，保留/覆盖策略和内容比较通过；华为云也完成本机隔离恢复 |
| Nginx 实际恢复 | 两机独立配置、PID 和 loopback HTTPS 端口；有效证书、相对链接重试、错误配置回滚后 HTTPS 均通过，未重载生产 Nginx |
| Compose 实际启动 | 两机现有镜像的专用容器；启动与健康等待、再次调用不重建、活动挂载拦截、unhealthy 拒绝均通过；生产项目仅做只读检查 |
| Vaultwarden 实际恢复 | 两机专用 named volumes；配置替换保留其他 remote、加密 ZIP 恢复、错误密码不改原数据、活动卷拦截均通过；生产卷未参与 |

两机各 4 组实际集成检查最终全部通过。腾讯云重复测试曾因旧夹具 SQLite 表已存在而失败，清理单个测试数据库后重跑通过。没有拉取新镜像；华为云剩余空间约 265 MiB，没有进行大数据恢复、重启或断电测试。

### 生产状态与限制

- 两机生产容器 ID、镜像、运行状态、启动时间、重启计数与基线一致。腾讯云 12 个生产容器运行；华为云保留原先 4 个运行容器，其余未启动。
- 业务 Compose、Nginx 配置/证书、UFW、SSH、cron 和已有备份脚本哈希均未变化；腾讯云 Vaultwarden 配置卷也未变化。
- 唯一观察到的配置变化是腾讯云主机 `/root/.config/rclone/rclone.conf`，mtime 为 `2026-09-08 14:43:46 UTC`，Outlook token 的当前有效期为 `2026-09-08 23:43:45 +08:00`。缺少原始写入追踪，不能确认写入来源，也未回退可能刷新的凭据。随后逐 remote 复验，配置枚举及 4 次隔离检测均确认原文件哈希不变。
- 两机 4 个主机 remote 均读取有效；腾讯云 Vaultwarden 实际配置读取有效。华为云缺少备份容器，不能由其环境确定备份 remote，报告无法检测，不当作凭据已失效。
- 腾讯云 CPA 和 sub2api 的容器到宿主机代理 TCP 检查通过。华为云 Emby 的 `172.26.0.1:42005` 不可达，已报告，未修改代理或防火墙。
- 13 个生产域名 HTTPS 检查均通过证书验证。`cpa`、`mcphub`、`mypassword`、`new-api`、`openlist`、`sub2api` 返回 200；`SearchMcpHub` 返回 302、`cpa-manager` 返回 307；`syncclipboard` 和 `mihomo` 未认证返回 401。没有执行用户登录，也不将 200/401 当作登录和数据完整性验证。
- `emby`、`QMediaSync`、`model-detect` 仍返回 502：腾讯云相关上游 `127.0.0.1:38095`、`:33333`、`:20020` 不可达。Emby/QMediaSync 未在腾讯云运行；model-detect 虽有 `/root/model-detect/deploy/model-detect.service`，系统 service 为 `not-found`。这属于未启动业务或未安装系统服务，未越权启动，复制 `/root` 或 Compose 一键启动不能覆盖这种 systemd 部署。
- 未实测生产备份写入、云安全组修改、系统包安装失败及跨重启自启；对应安全失败路径以隔离回归验证。目录恢复不能补回原备份从未包含的 root 层文件、隐藏配置、外部 binds、volumes、UID/GID 或系统 cron。

### 清理

核对绝对路径、所有容器挂载、系统挂载、cron 和进程引用后，删除腾讯云旧 agent 目录 `/root/vaultwarden/migration-20260908`、`/root/syncclipboard/migration-20260908`、`/root/linux-daimon/audit-20260907`，合计约 211 MiB。源服务器原数据、生产 volumes 和云端历史 ZIP 保留。两机本轮专用容器/卷、HTTP/Nginx 测试进程及 SSH 隧道已清理，隔离任务目录也已移除。少量脱敏结果与基线保留在本地被忽略的 `.tmp/migration-code-test-20260908/`。
